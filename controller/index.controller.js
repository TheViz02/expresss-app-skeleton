export const indexFunction = (req, res) => {
  res.status(200);
  res.json({ title: "Express" });
};

export const secondFunction = (req, res) => {
  res.status(200);
  res.json({ title: "Express" });
};
